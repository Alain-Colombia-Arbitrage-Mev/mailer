export default defineNuxtPlugin(async () => {
  // PLUGIN TEMPORALMENTE DESACTIVADO para evitar conflictos de redirección
  console.log('🚫 Plugin admin-auth-init DESACTIVADO temporalmente')
  return
})
